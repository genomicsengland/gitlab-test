stages:
  - run_tests

run_dq_tests:
  stage: run_tests
  image: registry.gitlab.com/genomicsengland/dataquality/cdt-docker/cdt-docker:latest
  script:
    - cp ${cdt_del_deploy} ${CI_PROJECT_DIR}/.gel_config
    - ls -lh .gel_config
    - cat .gel_config
    - Rscript test.r
  tags:
    - ou:cln
  only:
    refs:
      - master

